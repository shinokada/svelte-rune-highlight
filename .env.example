# Private environment variables (not exposed to the browser)
# Google Analytics ID for Runatics
ANALYTICS_ID=your_google_analytics_id

# Public environment variables (exposed to the browser via import.meta.env.PUBLIC_*)
# Carbon Ads configuration (optional - only needed if using Carbon Ads)
# PUBLIC_CARBON_ZONE_ID=your_zone_id
# PUBLIC_CARBON_PLACEMENT=yoursitecom

# Fathom Analytics (optional - alternative to Google Analytics)
# PUBLIC_FATHOM_SITE_ID=your_fathom_site_id
